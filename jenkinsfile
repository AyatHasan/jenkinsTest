pipeline {
    agent any
    environment {
        ENV = "testing"
    }
    stages {
        stage("Test") {
        
            steps {

    def jobName = "${env.JOB_NAME}"
          def environment = jobName.split("-")[1]
          
          echo ${environment}

            }
        }
    }
}
