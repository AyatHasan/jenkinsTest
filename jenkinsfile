pipeline {
    agent any
    environment {
        ENV = "testing"
    }
    stages {
        stage("Test") {
        
            steps {
                echo "Test stage."
                disableConcurrentBuilds()

            }
        }
    }
}
