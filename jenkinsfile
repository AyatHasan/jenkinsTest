 def jobName = "${env.JOB_NAME}"
          def environment = jobName.split("-")[1]
pipeline {
    agent any
    stages {
        stage("Test") {
        
            steps {

  
          
echo"${environment}"
            }
        }
    }
}
